import { SectionPage } from '../../_components/section-page';

export default function ModerationSpamPage() {
  return (
    <SectionPage title="Chống spam" description="Chỉ còn policy spam chuyên sâu trên một page riêng.">
      <p className="muted">Nơi đặt burst rule, link rule, bot rule, bio scan và action presets.</p>
    </SectionPage>
  );
}
